import React from 'react';
import { BotMessage } from '@/types';

export const botCallWaiterMessage: BotMessage = {
  1: {
    text: <span>What is your table code? Look at corner of the table😊</span>,
  },
};
