describe('Order', () => {
  it('Make a successfully order with a reservation before', () => {
    cy.visit('http://localhost:3000');
  });

  it('Make a takeaway order', () => {
    cy.visit('http://localhost:3000');
  });

  it('Make an order with 0 dish', () => {
    cy.visit('http://localhost:3000');
  });

  
});
